#!/bin/bash

echo "🔧 Fixing backend package.json..."

# Create corrected package.json
cat > package.json << 'EOF'
{
  "name": "auth-backend",
  "version": "1.0.0",
  "description": "Authentication backend with OTP",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "axios": "^1.12.2",
    "bcryptjs": "^2.4.3",
    "cloudinary": "^2.7.0",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.21.2",
    "express-rate-limit": "^8.1.0",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.19.1",
    "multer": "^1.4.5-lts.1",
    "streamifier": "^0.1.1",
    "twilio": "^5.9.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
EOF

echo "✅ Package.json fixed!"
echo "📦 Installing dependencies..."
npm install

echo "🚀 Ready for Render deployment!"
